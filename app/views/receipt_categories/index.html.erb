<h1> Receipt Categories </h1>

<%= link_to "Add Receipt category", new_receipt_category_path %><br>
<%= link_to "Receipts", receipts_path %>

<table>
  <tr>
    <th> Name </th>
  </tr>
  <% @receipt_categories.each do |category| %>
    <tr>
      <td><%= category.name %></td>
    </tr>
  <% end %>
</table>